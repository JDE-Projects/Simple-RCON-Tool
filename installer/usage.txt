Launch Simple RCON Tool from the Start menu, then click Add Server to pick a game and enter its host and RCON port.
On the server card, type the RCON password and click Connect, then use the command buttons or type a command in the box.
The password is held in memory only and wiped on disconnect; only the server name, host, port, game, and custom buttons are saved.
